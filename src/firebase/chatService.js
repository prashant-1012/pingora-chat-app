// Chat service — wraps all Firestore operations for conversations and messages.
// Populated fully in Phase 4 (1-on-1 chat) and Phase 5 (group chat).

// Placeholder exports to establish the module contract.
export const createConversation = async () => {};
export const sendMessage = async () => {};
export const subscribeToMessages = () => {};
export const subscribeToConversations = () => {};
